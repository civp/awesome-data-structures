#include "threadedBT.h"

ElementType addKey(ElementType key, ThreadedBT root)
{
	
}
ElementType deleteKey(ElementType key, ThreadedBT root);
ThreadedBT findKey(ElementType key, ThreadedBT root);
void inorderTraverse(Operation visit, ThreadedBT root);
Operation print;

#ifdef TEST
int main(int argc, char const *argv[])
{
	/* code */
	return 0;
}
#endif